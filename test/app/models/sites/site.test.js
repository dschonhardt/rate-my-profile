var sandbox;

var site = require('../../../../app/models/sites/site');

describe('site', function () {
	beforeEach(function () {
		sandbox = sinon.sandbox.create();
	});

	afterEach(function () {
		sandbox.restore();
	});

	// TODO

});